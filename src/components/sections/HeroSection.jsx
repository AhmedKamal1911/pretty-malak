// Import Swiper styles

import { HeroSlider } from "..";

const HeroSection = () => {
  return (
    <section className="lg:h-[calc(100vh-75.97px)]">
      <HeroSlider />
    </section>
  );
};

export default HeroSection;
