const TripDetailsBox = ({ detailsList }) => {
  const { names, tripDetails } = detailsList;
  return (
    <div className="p-3">
      <ul className="flex"></ul>
    </div>
  );
};

export default TripDetailsBox;
